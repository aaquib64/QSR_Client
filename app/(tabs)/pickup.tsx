import { View, Text } from 'react-native'
import React from 'react'

export default function pickup() {
  return (
   <View className="flex-1 justify-center items-center">
      <Text className="text-lg text-blue-500">Pickup Screen</Text>
    </View>
  )
}